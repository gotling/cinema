#!/bin/bash

/usr/bin/fbi -d /dev/fb0 --noverbose --autozoom --timeout 60 /home/pi/posters/*

# Restart slideshow if killed with signal QUIT (42)
RETVAL=$?
[ $RETVAL -eq 42 ] && ~/cinema/bin/showimage
